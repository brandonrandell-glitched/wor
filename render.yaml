# Render Blueprint — connect repo at https://dashboard.render.com
# New → Blueprint → select this repository (uses render.yaml automatically).

services:
  - type: web
    name: gtm-agent-ecosystem
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "8080"
      - key: FLASK_DEBUG
        value: "0"
